## 심사 arch
- delivery: 63점
  강점: 단계마다 '지금 배포하면 무엇이 동작하는가'를 명시한 수직 슬라이스 구성 — 세 계획 중 유일하게 인프라를 '동작하는 화면의 부산물'로 만든다. P0 종료 시 사장님이 눈으로 확인할 산출물이 있는 유일한 계획.; app/[locale] 세그먼트를 P0-6에 넣고 EN 번역은 비운 판단이 정확하다. 세 계획 중 유일하게 i18n 라우팅을 URL이 굳기 전에 확정한다(다른 둘은 이 구멍을 못 봤다).; 목업 결합을 토큰·카피·데이터 3접점으로 좁히고, tokens.test.ts에 멱등성·누락검출까지 요구한 것은 세 계획 중 가장 기계적이다.; P1-4 risk에서 서버액션 본체를 순수 함수 + deps 주입으로 분리하고 'use server' 래퍼를 얇게 두라고 명시 — 서버액션 테스트 가능성을 계약 수준에서 다룬 유일한 서술.; P0-7에서 SVG 레이어와 라벨(HTML) 레이어를 컴포넌트 경계로 분리해 목업 변경 폭발반경을 국소화한 것, P1-6에서 접수 경로를 위저드 하나로 통일한 것 — 둘 다 옳은 경계 판단.
  치명결함: **전제 사실이 틀렸다.** P0-1은 Supabase 프로젝트를 '프로비저닝'하고 P0-4 risk는 '아직 프로덕션 DB가 없으므로 지금이 마지막 무통증 시점'이라고 쓰는데, 레저 기록상 원격 프로젝트는 이미 존재하고 0001이 SQL Editor로 적용됐으며 CLI link + migration repair까지 끝났다(.superpowers/.../progress.md). 즉 P0-4는 '빈 DB'가 아니라 이력 동기화된 라이브 원격 DB에 CHECK→FK 컬럼 교체를 치는 작업이고, 계획은 그 위험을 아예 인지하지 못한다. 리전 역시 이미 고정돼 있어 'ap-northeast-2로 만들라'는 지시는 실행 불가이거나 프로젝트 재생성을 요구한다.; **개인정보 생애주기가 절반만 있다.** P1-1은 /privacy에 보유기간을 게시하게 하고 P1-2는 동의 컬럼 6개를 만들지만, retention_until 컬럼도 파기 배치도 계획 전체에 없다. 게시한 보유기간을 이행할 수단이 없으므로 그 고지는 오픈 첫날부터 허위 표시이고, PIPA §21 파기 의무 이행 경로가 0이다. legal·risk 둘 다 갖고 있는 것을 유일하게 빠뜨렸다.; **읽기 함수를 서버액션에 넣었다.** 계약이 `actions/showcase.ts — getShowcaseRoutes()`인데, 'use server' 모듈의 export는 전부 공개 POST RPC가 된다. RSC에서 쓰는 캐시 읽기를 굳이 공개 엔드포인트로 만들고 unstable_cache와 액션 직렬화를 한 모듈에 섞는 구성 — 최상급 구현가가 보면 부끄러운 배치다(risk·legal은 lib/showcase.ts에 올바르게 뒀다).; **통지 설계가 자기모순이다.** P2-2 테스트 (3)은 'reservation_id+event 중복 발송을 유니크로 차단'을 요구하는데 P3-3은 '재발송 버튼'을 요구한다. 게다가 0001의 notifications_log.status CHECK는 ('sent','failed') 2값뿐이고 계획 어디에도 이를 확장하는 마이그레이션이 없어 '기록 후 발송'(pending)이 구조적으로 불가능하다. after() 실패가 조용히 사라지는 경로를 스스로 지적해놓고 스키마로 막지 않았다.; **admin이 계속 service role 위에 선다.** P3-2는 requireAdmin() 이후 service role 클라이언트 생성을 정상 경로로 규정하므로 RLS가 최종 방어선이 아니고, 방어선은 'export 함수 파싱 테스트' 하나다. 자기진단에서 그 취약함을 인정하면서도 설계를 바꾸지 않았다. 더해 actions/admin.ts 하나에 12개 함수를 몰아넣고 P3-3~P3-6 네 태스크가 같은 파일을 순차 편집한다 — SDD 직렬 병합에서 충돌이 예정된 구조.; P0-8의 risk('public/brand/*.png가 미커밋이라 Vercel 404')는 사실이 아니다 — git status가 clean하고 자산은 이미 추적 중이다. 마찬가지로 P0-2는 styles/tokens.css를 새로 만든다고 서술하지만 그 파일은 이미 존재하고 app/layout.tsx가 import하고 있다. 저장소 실측 없이 쓰인 흔적이 태스크 두 개에 남아 있다.
- risk: 73점
  강점: 비가역성 기준 정렬이 세 계획 중 유일하게 일관된다 — 복구 불가(도메인·메일·백업) → 소급 불가(동의 컬럼·인증 경계) → 재작업(스키마) → 화면. 각 태스크 risk에 '언제/비가역성/제거' 3항을 강제한 형식도 검토 가능성이 높다.; **게이트를 코드보다 먼저, 조건부 강제로 심는다.** R0-2의 '대상 파일 부재 시 exit 0, 생기는 순간부터 실패'는 나중에 게이트를 붙일 때 생기는 '이미 위반된 코드를 통과시키는' 사고를 구조적으로 없앤다. 픽스처 양성/음성 2회 실증까지 요구한 것도 옳다.; **폭발반경 축소가 유일하게 실질적이다.** A1-1에서 admin을 service role에서 떼어 is_admin() RLS로 옮기고, admin 경로에 createServiceClient() 호출 0건을 grep으로 강제한다. RLS를 장식이 아니라 최종 방어선으로 만든 유일한 계획.; R0-4의 server-only 경계 스캐너('use client' 파일의 server.ts import 0건, NEXT_PUBLIC_ 접두 시크릿 0건)와 실패 케이스를 실제 red로 만들어보라는 지시 — 게이트 자체를 검증하는 유일한 서술.; D4-1 아웃박스 설계가 세 계획 중 유일하게 정확하다: status pending/sent/failed 확장 + attempts + last_error + `unique(reservation_id,event,channel) where status='sent'` 부분 인덱스. '기록 후 발송'과 '재발송 허용'을 동시에 만족시키는 유일한 구성.; C3-2: public_code를 비순차로 강제하고 TZ=UTC/Asia/Seoul 양쪽에서 날짜 경계를 실증 — 열거 공격과 KST 경계를 동시에 막는 유일한 태스크.
  치명결함: **[locale] 세그먼트가 계획 전체에 없다.** F6-2가 '/와 /en 200'을 검증하겠다면서 파일은 app/layout.tsx·app/(site)/page.tsx·app/privacy/page.tsx이고, next-intl의 as-needed 프리픽스는 app/[locale] 세그먼트를 요구한다. B2-3에서 /privacy·/terms·/guide를 P2에 먼저 만들고 F6에서 i18n을 배선하므로, EN을 켜는 순간 이미 존재하는 십수 개 라우트 파일을 전부 이동해야 한다 — 이 계획이 '늦게 발견하면 전량 재작업'이라 부른 바로 그 부류를 스스로 만들었다.; **P3 게이트가 실행 불가능하다.** '프리뷰 URL에서 실접수 1건이 DB 행 + public_code로 관측'을 P3 수용 기준으로 걸어놓고, 접수 폼(F6-5)은 세 단계 뒤 P6에 있다. UI 없이 액션만 배포한 상태에서 이 게이트를 통과시킬 방법이 없다 — 결국 게이트를 건너뛰거나 임시 폼을 만드는데, 어느 쪽이든 계획의 검증 체계가 첫 공개 뮤테이션에서 무너진다.; **copy-parity 테스트가 세션 경계를 침범한다.** F6-1은 목업 HTML에서 회사정보 8필드·verbatim 2문구를 추출해 messages/ko.json과 문자열 일치를 강제한다. 목업은 UIUX 세션 소유이므로, 그쪽의 마크업 리팩터 한 번이 구현 세션 CI를 red로 만들고 그 red는 구현 세션 파일을 고쳐서는 해소되지 않는다. 단방향 계약이라 주장하지만 실제로는 남의 커밋이 내 CI를 깨는 결합을 새로 만든 것이고, HTML 파싱 기반이라 오탐이 상시화된다.; **admin RLS 전환에 폴백이 없다.** 자기진단에서 '집계·크로스테이블 조회에서 정책에 걸려 service role로 되돌아가는 경우가 흔하다'고 인정하면서, 되돌아갈 때의 설계(어느 쿼리를 SECURITY DEFINER 뷰/RPC로 뺄지)를 계획에 넣지 않았다. P1의 명분 전체가 검증되지 않은 가정 위에 서 있다. 동시에 공개 insert 경로는 여전히 service role인데, insert 전용 RLS 정책이나 RPC로 그 마지막 한 곳까지 줄이는 선택지는 검토조차 없다.; **P0~P5, 태스크 21개 동안 사장님이 볼 화면이 0이다.** D-29 도메인 시계가 도는 상황에서 첫 공개 화면이 6번째 단계에 있고, F6-4(홈 이식)는 6개 섹션을 한 태스크에 묶어 스스로 '서브에이전트 한 번에 끝나지 않을 가능성이 높다'고 인정한다. 앞단을 안전하게 만든 대가를 뒷단 한 덩어리에 몰아둔 구조는 delivery와 같은 병이고, 여기서는 그 덩어리가 더 크다.; F6-2가 Header를 app/layout.tsx에 넣는데 A1-3이 만든 app/admin/layout.tsx는 그 루트 레이아웃 아래에 중첩된다 → 관리자 화면이 공개 사이트 헤더/푸터를 상속한다. app/(site)/ 그룹을 F6-4에서야 쓰기 시작하면서 셸 소유권이 두 번 결정된다.
- legal: 65점
  강점: **lib/legal/disclosures.ts를 '함수 export 0개'로 못박고 그것을 테스트로 검증**하는 설계는 전자상거래법 §13②3호 이행과 가격 계산 코드 금지를 양립시키는 가장 깔끔한 경계다. check-no-pricing(있으면 안 되는 것)과 check-legal-disclosures(있어야 하는 것)를 반대 방향에서 잠근다는 서술도 정확하다.; 법적 사실을 원장 모듈 하나로 격리하고 컴포넌트에는 한글 리터럴을 금지(게이트로 검사)한 것 — 목업이 어떻게 바뀌어도 법정 문구가 스킨과 함께 날아가지 않는 유일한 구조.; L9/H8-3의 파기 배치를 dry-run 기본값으로 두고, 확정건은 전자상거래법 보존기간으로 별도 계산해 조기 삭제를 막는 경계 테스트 — 세 계획 중 파기 오작동의 비가역성을 제대로 다룬 서술.; 픽셀 이식 브리프에 **목업 커밋 해시를 고정**하고 이후 해시 구간 diff만 재적용하는 후속 태스크를 만든다는 규약 — 목업 추종을 '테스트로 감시'가 아니라 '버전으로 고정'으로 푼 유일한 아이디어이며 risk의 copy-parity보다 결합이 약하다.; CONTACT_METHODS 상수가 위저드 칩의 유일한 진실이고 '목업이 아니라 코드가 진실'이라고 선언한 것(C3) — 지키지 못할 약속(이메일 견적)을 데이터 계약 수준에서 제거한다.; '개발 준비 완료'와 '오픈 가능'을 분리해 서명한다(E6)는 규약 — 사장님 의존 항목이 절반인 런치 게이트를 정직하게 다룬다.
  치명결함: **받지 않은 동의의 타임스탬프를 기록한다.** L6은 '필수 동의 체크박스 0개(계약 이행 근거 설계)'를 테스트로 강제하는데, L3은 privacy_consent_at 컬럼을 만들고 L7은 'insert payload에 privacy_consent_at이 채워짐'을 성공 조건으로 삼는다. 동의를 받은 적이 없는데 동의 시각을 남기는 것은 입증책임 대응이 아니라 허위 기록이다. 계약 이행 근거로 가겠다면 컬럼명·의미는 notice_shown_at이어야 하고, 동의 근거로 가겠다면 체크박스가 있어야 한다 — 두 설계가 한 계획 안에서 충돌한다.; **법정 라우트를 i18n보다 먼저 만들어 자기 목적을 파괴한다.** L5가 app/privacy·app/terms·app/guide를 루트에 만들고, B6에서 next-intl을 배선한다. [locale] 세그먼트가 계획 어디에도 없어 EN을 켜는 순간 이 3라우트를 포함한 전 라우트를 이동해야 하며, '법정 코어를 먼저 굳힌다'는 전략의 산출물이 가장 먼저 재작업 대상이 된다. 게다가 법정 문서 URL이 바뀌면 이미 발송된 문자(L8이 /guide URL을 본문에 넣는다)의 링크가 깨진다.; **admin 쓰기 경로가 자기모순이다.** D2는 'showcase_routes·notices·gallery·popups의 쓰기가 관리자에게만 허용'됨을 RLS 테스트로 검증하라 하고, D1의 risk는 '읽기는 SSR 세션, 쓰기만 검증 후 service role'이라 못박는다. service role은 RLS를 우회하므로 D2가 검증하는 쓰기 정책은 실제 코드 경로가 한 번도 통과하지 않는 장식이 된다. RLS를 방어선으로 삼는 척하면서 실제 방어는 requireAdmin() 하나뿐이다.; **레이아웃 소유권이 두 번 결정된다.** L4가 푸터를 app/layout.tsx에 붙이고 B1이 app/(site)/layout.tsx를 새로 만든다. 그 사이 D1의 app/admin/layout.tsx는 루트 아래에 중첩되므로 관리자 화면이 공개 마케팅 푸터(계좌번호·사업자정보 포함)를 상속한다. 셸 경계를 정하지 않은 채 법정 블록을 먼저 심은 대가.; **중간 산출물이 배포 가능하지만 무용하다** — 자기진단이 인정한 그대로다. 태스크 35개, Phase 6개에 첫 공개 화면(B1)이 10번째 태스크이며, D-29 도메인 시계와 SDD 루프(브리프→구현→리뷰→픽스)를 감안하면 일정 초과가 계획 자체의 구조에서 나온다. Phase L만으로는 '법적으로 오픈 가능'하지만 사장님에게 보여줄 것이 없어 컨펌 루프가 돌지 않는다.; actions/admin.ts 하나를 D3·D4·D5가 순차 편집하는 구조(delivery와 동일한 병), 그리고 D6의 이미지 변환 3종은 sharp 등 스택에 없는 의존성과 Vercel 함수 제약을 검증하지 않은 채 다른 태스크와 같은 크기인 척한다 — 자기진단이 지적했으나 계획은 그대로 두었다.
이식할 아이디어: [delivery] app/[locale] 세그먼트를 첫 라우트 파일보다 먼저 확정하고 EN 메시지는 비워 ko 폴백으로 둔다(localePrefix 'as-needed'). risk·legal은 이 구멍을 못 봤고, 법정 3라우트·admin·문자 본문 링크가 전부 URL에 묶이므로 이건 선택이 아니라 선행 조건이다. / [delivery] 단계 목표를 '지금 배포하면 무엇이 동작하는가' 한 문장으로 쓰는 규약 + 홈 1페이지 수직 슬라이스를 최초 산출물로 두는 순서. risk/legal의 '보이지 않는 앞단 4~6단계'를 이것으로 잘라야 D-29 시계 안에서 사장님 컨펌 루프가 돈다. / [delivery] 서버액션 본체를 deps 주입 순수 함수로 분리하고 'use server' 래퍼를 얇게 유지 — 세 계획 중 유일하게 서버액션 테스트 가능성을 계약으로 다룬 서술. 여기에 next/cache·next/server 원시값(after/revalidateTag/cookies)을 감싸는 포트 모듈을 더해야 완성된다. / [delivery] KrMap을 SVG(지도) 레이어와 HTML(라벨) 레이어로 분리해 목업 변경 폭발반경을 라벨 레이어에 가둔다. 16노선 라벨 겹침 미해결 문제의 유일하게 현실적인 흡수 지점. / [delivery] 접수 경로를 위저드 하나로 통일하고(홈 위젯은 프리필만) 목업 동작 축소를 사장님 고지 항목으로 승격 — 동의 UI·검증이 두 벌 생기는 사고를 설계로 제거한다. / [risk] 게이트를 코드보다 먼저, '대상 파일이 생기는 순간부터 실패'하는 조건부 강제로 심는 방식(check-legal-disclosures.sh) + 게이트 자체를 양성/음성 픽스처로 실증하라는 요구. / [risk] admin을 service role에서 떼어 is_admin() RLS로 옮기고 'admin 경로에 createServiceClient() 호출 0건'을 grep으로 강제. 단 되돌아갈 때의 설계(집계·크로스테이블은 SECURITY DEFINER 뷰/RPC로 격리)를 폴백으로 함께 명시할 것. / [risk] notifications 아웃박스 스키마: status pending/sent/failed + attempts + last_error + `unique(reservation_id,event,channel) where status='sent'` 부분 인덱스. '기록 후 발송'과 '명시적 재발송'을 동시에 성립시키는 유일한 형태 — delivery의 유니크/재발송 모순을 이걸로 해소. / [risk] R0-4 server-only 경계 스캐너('use client' 파일의 server.ts import 0건, NEXT_PUBLIC_ 시크릿 0건)와 '실패 케이스를 만들어 테스트가 실제로 red가 되는지 확인' 규약. / [risk] public_code 비순차 생성 + TZ=UTC/Asia/Seoul 양쪽에서 날짜 경계 실증(열거 공격 + KST 자정 경계 동시 차단). / [legal] lib/legal/disclosures.ts를 '함수 export 0개' 정적 상수로 못박고 그것을 테스트로 검증 — §13②3호 이행과 가격 계산 코드 금지를 양립시키는 경계. 반대 방향 두 게이트(있으면 안 되는 것 / 있어야 하는 것)로 잠그는 구성까지 그대로. / [legal] 파기 배치를 dry-run 기본값으로 출시하고, 확정건은 전자상거래법 보존기간으로 별도 계산해 조기 삭제를 막는 경계 테스트. delivery에는 파기 자체가 없으므로 반드시 이식해야 한다. / [legal] 픽셀 이식 브리프에 목업 커밋 해시를 고정하고 이후 해시 구간 diff만 재적용 — risk의 copy-parity(HTML 파싱)보다 결합이 약하면서 추종 누락을 잡는다. / [legal] 칩·드롭다운 항목은 lib/codes.ts 상수가 결정하고 목업이 아니라 코드가 진실(이메일 견적 칩 제거) + '개발 준비 완료'와 '오픈 가능'을 분리 서명하는 런치 게이트 규약.
전원 누락: **375px 가로 스크롤 게이트가 이미 무력화돼 있다.** app/globals.css:1-4의 `html, body { max-width:100vw; overflow-x:hidden }` 때문에 세 계획이 공통으로 쓰는 실측 기준(document.scrollingElement.scrollWidth === 375)이 클램프되어 오버플로가 있어도 통과한다. 어느 계획도 이 파일을 손대지 않는다 — 모든 UI 태스크의 주 게이트가 첫날부터 위양성이다. globals.css의 overflow-x:hidden 제거(또는 래퍼 요소 실측으로 전환)를 P0 태스크로 올려야 한다. / **커스텀 도메인 컷오버가 Server Action을 깨뜨리는 경로를 아무도 안 봤다.** Next 15의 Server Action은 Origin/x-forwarded-host 검증을 하며, bestour.co.kr로 A 레코드를 옮기고 프리뷰(vercel.app)와 병행 운영하는 순간 `serverActions.allowedOrigins` 설정 없이는 접수·admin 전 뮤테이션이 'Invalid Server Actions request'로 실패한다. 세 계획 모두 마지막 단계에서 DNS를 전환하면서 next.config.ts에 이 항목이 없고, 컷오버 리허설 체크리스트에도 '전환 후 실제 접수 1건'이 없다(리다이렉트 curl만 있다). / **프리뷰/스테이징 DB가 없다.** 원격 Supabase 프로젝트는 이미 하나뿐이고 link까지 끝난 상태인데, 세 계획 모두 파괴적 컬럼 교체 마이그레이션과 16노선 시드를 그 하나에 적용하고 Vercel 프리뷰도 같은 DB에 쓴다. Supabase 브랜칭 또는 프리뷰 전용 2번째 프로젝트, 그리고 프리뷰 접수가 운영 reservations를 오염시키지 않게 하는 분리가 어느 계획에도 없다. 사장님 E2E와 실접수가 같은 테이블에 섞인다. / **마이그레이션 롤백 전략이 없다.** 라이브 원격 DB(migration repair 이력 동기화됨)에 0002~0006을 순차 적용하면서 down 스크립트도, 적용 전 복원 지점(PITR/백업) 확인 절차도 없다. 특히 CHECK→FK 컬럼 교체는 실패 시 수동 개입이 필요한데 세 계획 모두 '로컬 db reset green'만 게이트로 삼는다. / **DB 스키마 ↔ TS 타입 드리프트 방어가 없다.** lib/types.ts는 손으로 유지되고 마이그레이션이 4~6개 추가되며 세 단계가 각각 컬럼을 더한다. `supabase gen types typescript` 생성물이나 스키마↔타입 정합 테스트가 어느 계획에도 없어, 컬럼명 오타가 런타임(그것도 insert 실패)까지 살아남는다. / **캐시된 폴백이 장애를 고착시킨다.** 세 계획 모두 getShowcaseRoutes를 unstable_cache(tag 'showcase', 60초)로 감싸고 그 안에서 DB 실패 시 가격 null 정적 카탈로그로 폴백하라고 지시한다. 캐시 함수 내부의 폴백 결과는 그대로 캐시되므로, 일시적 오류 한 번이 홈의 가격 라벨을 최소 TTL 동안(태그 무효화 전까지) 통째로 지운다. '실패 결과는 캐시하지 않는다'가 계약에 없다. 덧붙여 Next 15에서 unstable_cache의 위치('use cache'/cacheTag로 갈지)를 정한 계획도 없다. / **Next 런타임 원시값의 테스트 이음매가 없다.** after()·revalidateTag()·cookies()는 vitest에서 요청 스코프 없이 호출하면 던지거나 무의미하다. 그런데 delivery P2-2(1), risk D4-3, legal C2가 전부 'after()가 응답을 막지 않는다'를 vitest로 검증하겠다고 쓴다. 이 테스트들은 그대로는 작성 불가이거나 공허하다 — lib/runtime.ts 같은 포트 모듈로 감싸 주입하는 경계를 세 계획 어디도 정의하지 않았다. / **공개 insert 경로가 계속 service role 위에 있다.** risk가 admin을 RLS로 옮겨도 createReservation은 여전히 RLS 우회 키를 쓴다. insert 전용 anon 정책이나 SECURITY DEFINER RPC로 '공개 경로는 쓰기만 가능하고 어떤 버그로도 reservations를 읽을 수 없다'를 만드는 선택지를 세 계획 모두 검토하지 않았다. 키 폭발반경을 줄인다면서 가장 노출된 경로를 남겨둔다. / **애플리케이션 레벨 오류 관측이 0이다.** notifications_log는 문자만 덮는다. 서버액션 예외, RSC 렌더 실패, after() 콜백 예외, 미들웨어 오류를 알 방법이 세 계획 어디에도 없다(Sentry·구조적 로깅·Vercel 로그 드레인 전부 부재). 사장님이 '문자가 안 온다'를 발견하는 시점이 늦을 수 있다는 delivery의 자기진단은 사실 문자에 국한된 문제가 아니다. / **홈 '실시간 접수 현황' 공개가 개인정보 이용 목적으로 고지되지 않는다.** 세 계획 모두 마스킹된 이름 + 차량 + 날짜를 불특정 다수에게 상시 게시하게 하면서, /privacy의 처리 목적·항목 고지에 '접수 현황 공개 노출'을 넣지도, 거부 수단을 두지도 않았다. 법정 표시를 최우선으로 삼은 legal 계획조차 B7에서 이를 순수 기술 태스크로만 다룬다. / **styles/tokens.css의 기존 상태를 아무도 인지하지 않았다.** 그 파일은 이미 손으로 작성돼 있고 app/layout.tsx가 import한다. 세 계획 모두 '생성한다'고만 쓰고 기존 파일에서 스크립트 생성물로 넘어가는 이행(포맷 차이, 첫 실행 시 대량 diff, app/globals.css와의 역할 분담)을 정의하지 않았다.

## 심사 exec
- delivery: 58점
  강점: 세 계획 중 유일하게 매 단계가 '사람이 볼 수 있는 것'을 낸다. P0 프리뷰 링크가 그 자체로 '가격 산정 기준이 45인승 당일왕복이 맞느냐'는 미회신 질문을 사장님께 던지는 수단이 되어, 미회신 항목을 계획 밖으로 미루지 않고 앞으로 당긴다.; 법정 3라우트를 처음부터 app/[locale]/ 아래 둔 유일한 계획. risk(B2-3)·legal(L5)은 app/privacy에 만든 뒤 나중에 i18n을 붙여 라우트 이동·푸터/sitemap 링크 재작업이 확정적으로 발생하는데, 이 계획만 그 재작업이 0이다.; 수용 기준이 브리프에 그대로 복사 가능한 형태로 구체적이다 — '핀 개수 = DB 활성 노선의 고유 장소 수', 'price_from을 NULL로 바꾸면 그 라벨만 사라지고 핀·곡선은 남음', "'99-19' 문자열이 저장소 어디에도 없음을 grep", '하나은행 계좌 문자열 부재'. 리뷰어가 판정에 재량을 쓸 여지가 가장 적다.; 목업 추종을 3접점(토큰 자동추출·messages 카피 외부화·데이터는 DB)으로 좁히고, 픽셀 이식 태스크(P4-1·P4-2)는 '목업 동결 선언 후에만 착수'라는 시점 규칙을 명시. 병행 세션 충돌을 구조로 막으려는 시도가 가장 명확하다.; P3-2의 '모든 admin 서버액션이 requireAdmin()을 호출하는지 소스 파싱으로 검출하는 정적 테스트'는 레이아웃 가드가 구조적으로 취약한 상황에서 유일한 기계적 방어선이고, 실제로 작성 가능한 형태다.
  치명결함: [P0-3] 수용 기준이 자기모순이라 서브에이전트가 완수할 수 없다. '모든 장소 좌표가 문서화된 선형변환식 계산값과 0.1px 이내 일치'와 '기존 REGION_POINTS·map-coords 42개 테스트 무회귀'를 동시에 요구하는데, lib/map-coords.ts:20-30에 기록된 대로 GW 앵커(354.0,88.8)는 이 식과 x≈95px 이탈한다(실측 핀이 춘천이 아닌 강릉 위경도). 구현자는 무한 루프에 빠지거나 픽셀 검증된 앵커를 몰래 바꾼다. 게다가 신규 도시 '강릉'을 추가하면 GW 핀과 좌표가 정확히 중복되는데 이 계획만 이 사실을 언급조차 하지 않는다(legal M1은 잡아냄).; 저장소 실측과 어긋나는 전제 위에 P0 전체가 서 있다. (a) P0-4 risk '아직 프로덕션 DB가 없으므로 지금이 마지막 무통증 시점' — 레저상 원격에 0001이 적용되어 showcase_routes 5행이 있고 CLI link·migration repair까지 끝났다. (b) P0-8 risk 'public/brand/*.png가 미커밋(untracked)' — git ls-files로 확인한 결과 이미 트래킹·커밋되어 있고 워킹트리는 clean. (c) P0-1은 이미 존재하는 Supabase 프로젝트·.env.local 3키를 다시 프로비저닝하게 만든다. 이 전제로 브리프를 쓰면 구현자가 없는 문제를 풀거나 멈춘다.; P0 게이트 전체가 태스크 1번에서 사장님 의존으로 막힌다. P0-1이 '대표자 이승묵 명의 가입 + 사장님 직접 결제수단 등록'을 요구하고, P0 게이트는 'Vercel 프리뷰 URL에서 browse 실측 3종'이다. 즉 '수직 슬라이스가 서면 인프라는 부산물'이라는 전략 자체가 미회신 항목에 걸려 첫 태스크부터 정지할 수 있다. 폴백(개발자 개인 계정)을 적었지만 그 경우 P5 전 소유권 이관이라는 되돌리기 어려운 부채가 생긴다.; 30개 태스크 어디에도 retention_until 컬럼과 파기 배치가 없다. 그런데 P1-1은 /privacy에 '처리 항목·보유기간'을 게시한다 — 아무것도 강제하지 않는 보유기간을 게시하면 그 고지 자체가 거짓 표시가 되고 PIPA §21 파기 의무도 미이행이다. risk(H8-3)·legal(L9)에는 dry-run 포함 파기 배치가 있다. 같은 맥락으로 결정 메모가 명시적으로 지시한 scripts/check-legal-disclosures.sh CI 게이트도, 관리자 조작 감사 로그도 없다.; [P3-2] 통과 불가능한 테스트를 또 만든다. '미들웨어가 /admin을 매처에서 제외한 채로도 세션 쿠키가 갱신되는지 실측'이라고 적었지만, middleware.ts:22의 matcher `/((?!api|admin|_next|.*\..*).*)`는 /admin에서 미들웨어를 아예 실행시키지 않는다. 실행되지 않는 코드가 쿠키를 갱신할 수 없다. risk A1-2는 이 matcher를 고치는 것을 태스크로 삼는데 이 계획은 문제를 인지하지 못했다.; [P4-1] 태스크 크기가 다른 태스크의 5배다. 히어로 캐러셀 + 이용방법 4단계 + 차량 5종 + 강점 + 갤러리 프리뷰 + 공지를 한 태스크에 묶고 files에 components/sections/*.tsx라고 와일드카드를 썼다. 서브에이전트 1회로 끝나지 않는다. 더구나 착수 조건이 'UIUX의 목업 동결 선언'인데 그 선언의 주체·형식·시점이 계획 어디에도 정의되어 있지 않아 실질적으로 무기한 대기 태스크다.; [P1-3] fail-closed와 프리뷰 게이트가 충돌한다. 'Upstash env 미설정 시 통과가 아니라 실패' + '개발 환경 한정 우회 플래그가 NODE_ENV=production에서 무시됨'을 동시에 요구하는데, Vercel 프리뷰 빌드는 NODE_ENV=production이다. 사장님 명의 계정 대기 중에는 P1 게이트('프리뷰에서 실제 접수 1건 성공')가 영원히 닫히지 않는다.
- risk: 73점
  강점: '비가역성 순서'라는 정렬 축이 이 프로젝트에 실제로 맞다. 도메인 D-29·카페24 해지 시 MX 동반 소멸·그누보드 백업 7일 보관은 코드로 되돌릴 수 없고, 동의 컬럼 없이 받은 접수는 사후 보정이 불가능하다. 각 태스크 risk 항목이 '언제/비가역성/제거'로 표준화되어 우선순위 논쟁이 재발하지 않는다.; 게이트를 첫 UI 코드보다 먼저 심는 R0-2·R0-3·R0-4가 값싸고 효과가 크다. 특히 '대상 파일이 없으면 exit 0 + 경고, 생기는 순간부터 실패'라는 조건부 강제와 LAUNCH_GATE=1 강제 모드의 분리는 '나중에 게이트를 심으면 이미 위반된 코드를 통과시킨다'는 실제 사고 유형을 정확히 겨냥한다.; 보안 설계가 세 계획 중 유일하게 완결적이다. A1-2가 middleware.ts:22의 matcher가 /admin을 제외하고 있다는 사실을 실측으로 지적하고 그것을 고치는 것을 태스크로 삼는다(delivery는 이 사실을 인지하지 못한 채 통과 불가 테스트를 씀). 여기에 is_admin() SECURITY DEFINER + search_path 고정, admin_audit insert-only, server-only 경계 스캔까지 삼중.; 통지 설계가 가장 정확하다. D4-1의 status를 pending/sent/failed로 확장 + `unique(reservation_id,event,channel) where status='sent'` 부분 인덱스 + '기록 후 발송'은, 발송 직전 크래시나 after() 내부 예외가 무기록으로 사라지는 경로를 실제로 닫는다. legal C1(sent/failed 2상태)보다 한 단계 위다.; E5-2가 '두 핀 간 거리가 임계 이하일 때 라벨 충돌 플래그를 반환'하도록 데이터 레이어에 판정을 넣는다. 스펙 §13.2가 '16개 동시 표시 시 겹침, 별도 설계'라고 미해결로 남긴 문제에 대한 유일한 기계적 해법이고, UI가 결정만 하게 되어 목업이 바뀌어도 로직이 살아남는다.
  치명결함: P0~P5의 24개 태스크가 끝날 때까지 사람이 볼 수 있는 공개 페이지가 0이다. 각 단계 목표에 '배포 가능'이라고 적었지만 실제로 배포되는 것은 로그인 폼과 서버액션뿐이다. 도메인 만료 D-29와 사장님 컨펌 루프가 걸린 상황에서 첫 browse 가능한 홈이 26번째 태스크(F6-2)라는 것은 일정 리스크가 아니라 신뢰 리스크다 — 사장님이 진척을 확인할 산출물이 한 달간 없다.; 법정 라우트 배치가 스스로 재작업을 만든다. B2-3이 app/privacy/page.tsx에 3라우트와 푸터를 만들고, F6-2가 뒤늦게 next-intl 미들웨어와 app/layout.tsx를 배선한다. 그 순간 /privacy는 로케일 라우팅 밖에 남거나(EN 없음·구조 불일치) 경로를 옮겨야 하고, 이미 푸터·sitemap·문자 템플릿에 박힌 URL이 흔들린다. '늦게 발견하면 전량 재작업인 것을 먼저'라는 자기 원칙을 라우팅 계약에는 적용하지 않았다.; 병행 소유권 표가 실제 합의와 어긋나고 자기모순이다. UIUX 소유에 styles/tokens.css를 넣어놓고 F6-1에서는 구현 세션이 scripts/sync-tokens.sh로 그것을 재생성한다. 게다가 이미 발행된 docs/handoff/2026-09-06-uiux-session-brief.md는 UIUX에 mockups/만 주고 styles/·docs/superpowers/는 구현 세션 소유로 못박았다. 두 세션이 서로 다른 소유권 문서를 들고 같은 파일을 만지게 된다.; R0-2 법정 게이트가 fail-open이다. 'components/site/Footer.tsx가 존재하는데 호스팅/공정위 문자열이 없으면 exit 1' — 파일 경로를 하드코딩했으므로 구현자가 components/layout/Footer.tsx로 만들거나 라우트 그룹 이동으로 경로가 바뀌면 게이트가 조용히 통과한다. 계획의 중심 기제로 내세운 것이 이름 한 번 바뀌면 무력화된다. 디렉터리 전역 grep이었어야 한다.; 동의의 법적 근거를 결정하지 않은 채 서로 반대되는 두 브리프를 낸다. B2-4는 '필수 체크박스 0개(계약 이행 근거 설계)'를 요구하고, C3-3 계약은 `privacyConsent: z.literal(true)`를 강제하며 컬럼명은 privacy_consent_at이다. 근거가 계약 이행이면 기록해야 할 것은 '동의 시각'이 아니라 '고지 시각'이고, 근거가 동의면 체크박스가 필요하다. 구현자가 태스크 도중에 이 법적 판단을 혼자 내리게 된다.; 영문이 계획에서 사실상 증발했다. F6-2가 /en 200을 테스트하고 F6-1이 messages/en.json을 언급하지만, EN 콘텐츠를 실제로 만드는 태스크가 39개 중 0개다. 스펙 §11은 영문을 1차 범위로 확정했고 §13.3은 외국인 직접 문의가 '종종 있음'이라 비중 유지를 지시했다.; G7-3이 '1600/800/400 WebP 3종 생성'을 다른 태스크와 같은 크기로 배치하면서 변환이 어디서 도는지(sharp on Vercel / Supabase Image Transformation / 클라이언트 canvas)를 결정하지 않았다. 이 선택 하나가 태스크 크기를 3배까지 바꾸고 sharp는 현재 의존성에도 없다. delivery·legal은 최소한 자가진단에서 이 위험을 인정하는데 이 계획은 인지조차 하지 않는다.; R0-1이 이미 해소된 항목을 살아 있는 최상위 P0로 들고 있다. '게시판 337건 비공개 확인'은 사장님이 전화번호 노출 없음을 확인해 스펙 §13.11에서 종료된 사안이다. 사장님께 다시 요청하면 신뢰를 깎는다.
- legal: 66점
  강점: lib/legal/disclosures.ts를 '함수 0개 상수 모듈'로 규정하고 `Object.values(mod).every(v=>typeof v!=='function')`로 증명하는 설계는, 전자상거래법 §13②3호(가격 미결정 시 산정 방법 표시) 이행과 CLAUDE.md의 가격 계산 코드 금지를 충돌 없이 양립시키는 가장 정확한 봉인이다. 세 계획 중 이 경계를 테스트로 못박은 것은 이것뿐이다.; L8이 확정 문자를 §13② 후단의 '계약내용 서면'으로 설계하고, 광고 유발어('할인·이벤트·특가·프로모션')와 가격 숫자를 정규식으로 금지한다. 정보성 문자에 프로모션 한 줄이 섞이면 전체가 광고성으로 전환되어 정보통신망법 §50 요건이 붙는다는 메모의 지적을 유일하게 템플릿 계약으로 옮겼다.; M1이 세 계획 중 유일하게 'GW 앵커가 실제로는 강릉 좌표'라는 레저 기록을 인지하고, 신규 도시 '강릉' 코드와의 좌표 중복을 명시적 처리 대상으로 올렸다. delivery는 같은 지점에서 통과 불가능한 테스트를 만들었다.; Phase L 게이트가 사장님·외부 계정 없이 닫힌다 — 로컬 dev 서버 browse + 로컬 supabase 스택이면 충분하고 Vercel·Upstash·Turnstile·Solapi가 하나도 필요 없다. 세 계획 중 첫 게이트가 미회신 항목에 막히지 않는 유일한 계획이다.; 목업 대비 축소 결정을 리스크 주석이 아니라 태스크로 명문화한다 — C3(이메일 견적 옵션을 lib/codes.ts CONTACT_METHODS에서 제거해 '코드가 진실'로), B7(가짜 티커 금지·0건이면 섹션 숨김), E1(쿼리 기반 301 분기와 308 함정). 구현자가 목업을 보고 되살릴 여지를 없앤다.
  치명결함: [L5] 한 태스크가 /privacy·/terms·/guide 세 법정 문서 본문을 통째로 쓰게 한다. 특히 /terms는 전자상거래법 §10①5호 약관 본문이므로 서브에이전트가 계약 문서를 창작하게 되는데, 이는 이 프로젝트의 창작 금지 규약(L5)과 정면 충돌하고 태스크 크기도 명백히 초과한다. 세 계획 모두 변호사 검토 태스크가 없지만, '약관 생성'을 실제 태스크로 발주하는 것은 이 계획뿐이라 잘못된 안전감이 가장 크다.; 라우팅 계약을 나중으로 미뤄 스스로 재작업을 만든다. L4가 app/layout.tsx에 푸터를 붙이고 L5가 app/privacy에 라우트를 만든 뒤, B1이 app/(site)/layout.tsx를, B6이 next-intl 미들웨어를 넣는다. 라우트 그룹 이동과 로케일 세그먼트 도입이 확정적이며 이미 푸터·sitemap·문자 템플릿에 박힌 /privacy URL이 흔들린다. '법정 코어 먼저'라는 전략의 대가를 정작 가장 오래 사는 URL이 치른다.; 병행 소유권 표가 이미 발행된 인계 브리프와 충돌한다. UIUX 세션에 `docs/superpowers/specs/**`를 준다고 적었으나, docs/handoff/2026-09-06-uiux-session-brief.md는 UIUX에 mockups/만 주고 docs/superpowers/는 구현 세션 소유로 명시했다. 스펙 §13은 사장님 확정 사항의 원장인데 두 세션이 동시에 고칠 근거를 만든다.; 통지 설계가 sent/failed 2상태에 머문다(C1). 발송 직전 크래시·타임아웃·Solapi 무응답은 아무 행도 남기지 않고 사라지고, 재발송 액션·중복 발송 유니크 제약·attempts/last_error가 없다. C1 미개통으로 초기 발송이 확실히 실패하는 상황에서 '사장님이 접수를 통째로 놓친다'는 최악 시나리오의 탐지 수단이 없다. 관리자 조작 감사 로그도 없다.; 도메인 D-29가 35개 태스크 중 33번째(E3/E4)에서 처음 실체를 갖는다. 갱신은 사장님이 가비아에서 해야 하는 행위이고 실패하면 L~D의 모든 산출물이 갈 곳을 잃는데, 계획의 어떤 지점도 그 행위를 앞당기거나 게이트로 만들지 않는다. risk가 이것을 R0-1로 맨 앞에 둔 것과 정반대다.; [D6] 1600/800/400 WebP 변환본 생성을 전제하는데 sharp 계열 의존성이 package.json에 없고 Vercel 함수 메모리·실행시간 실측도 선행되지 않았다. 자가진단에서 인정했으면서 계획은 이 태스크를 다른 태스크와 같은 크기로 남겨뒀다 — 착수 시점에 재계획이 필요한 태스크가 정상 태스크로 위장해 있다.; L6(필수 체크박스 0개, 계약 이행 근거)과 L7(privacy_consent_at·marketing_consent_at 스탬프)의 근거가 어긋난다. 계약 이행 근거라면 기록 대상은 '동의 시각'이 아니라 '고지 시각·고지 버전'이어야 하는데 컬럼명과 계약이 동의 기반으로 되어 있어, 나중에 감독기관에 제출할 때 무엇을 입증하는 기록인지 설명할 수 없다.
이식할 아이디어: [risk R0-2/R0-3/R0-4] 게이트를 첫 UI 코드보다 먼저 CI에 심되 '대상 파일 부재 시 exit 0 + 경고, 생기는 순간부터 실패'라는 조건부 강제와 LAUNCH_GATE=1 강제 모드를 분리한다. 단 파일 경로 하드코딩(components/site/Footer.tsx)은 반드시 디렉터리 전역 grep으로 바꿔 fail-open을 없앨 것. / [risk A1-2] middleware.ts:22의 matcher `/((?!api|admin|_next|.*\..*).*)`가 /admin을 제외하고 있다는 실측 사실과, 그 제외를 걷어내는 것을 독립 태스크로 두는 것. 이 사실을 인지하지 못한 계획은 '미들웨어가 admin 세션을 갱신하는지 확인'이라는 통과 불가 테스트를 만든다. / [risk A1-1/A1-4] is_admin() SECURITY DEFINER + search_path 고정 RLS와 admin_audit insert-only 테이블. admin 조회를 service role에서 떼어 키 폭발반경을 '접수 insert 1곳'으로 줄이는 것, 그리고 관리자 상세 열람이 감사에 남게 하는 것. / [risk D4-1/D4-3/D4-4] notifications_log status를 pending/sent/failed로 확장 + attempts/last_error + `unique(reservation_id,event,channel) where status='sent'` 부분 인덱스 + '기록 후 발송' 순서 + admin 재발송 경로. 조용한 발송 실패를 잡는 유일한 구조. / [risk E5-2] 핀 간 거리 임계로 라벨 충돌 플래그를 좌표 모듈이 반환하게 해서, 16노선 동시 표시 겹침(스펙 §13.2 미해결)을 UI가 아니라 데이터 레이어에서 판정. / [legal L1/L2] 법적 사실을 함수 0개 상수 모듈(lib/legal/*)로 격리하고 `typeof v !== 'function'` 테스트로 증명. §13②3호 이행과 가격 계산 코드 금지의 충돌을 코드 수준에서 봉인하고, 목업 스킨이 바뀌어도 법정 문구가 살아남게 한다. / [legal L8] 확정 문자를 §13② 후단의 계약내용 서면으로 설계 + 광고 유발어·가격 숫자 정규식 금지 테스트. 정보성/광고성 전환 사고를 CI에서 차단. / [legal L9 + risk H8-3] retention_until 컬럼 + dry-run 기본값 파기 배치 + 확정건(전자상거래법 시행령 §6)과 미확정 접수건의 보존기간 분리 계산. delivery에는 통째로 없어 /privacy의 보유기간 고지가 거짓 표시가 된다. / [legal M1] 'GW 앵커의 실측 픽셀이 강릉 위경도'라는 레저 기록을 도시 카탈로그 설계에 반영해 GW와 신규 강릉 코드의 핀 중복을 명시적으로 처리. 이걸 놓치면 좌표 테스트가 통과 불가가 되거나 픽셀 검증된 앵커가 몰래 바뀐다. / [legal E1 / risk H8-1] 구 URL 301 매핑을 `has:[{type:'query',key:'bo_table',...}]`로 분기하고 statusCode:301을 명시(permanent:true 병용 금지, 308 아님), 테스트 선작성 후 구현. 네이버 색인 자산 보존의 유일한 기계적 담보. / [legal C3 + delivery P1-5] 위저드 '견적 확인 방법' 칩을 lib/codes.ts 상수가 결정하게 하고 이메일을 목록에서 제외 — 목업이 아니라 코드가 진실이 되게 하는 패턴. 다만 사장님 컨펌된 목업과 달라지므로 재컨펌을 태스크로 둘 것. / [delivery] 법정 3라우트와 푸터를 처음부터 app/[locale]/ 아래에 배치. risk·legal의 라우트 이동 재작업(푸터·sitemap·문자 템플릿에 박힌 URL 포함)을 통째로 없앤다. / [delivery P3-2(3)] actions/admin*.ts의 export된 'use server' 함수를 파싱해 requireAdmin() 호출 누락 시 실패하는 정적 검사 테스트. 레이아웃 가드가 구조적으로 누락에 취약한 상황의 기계적 방어선. / [delivery] 수용 기준을 관측 가능한 문장으로 쓰는 방식 자체 — '핀 개수 = 활성 노선의 고유 장소 수', 'price_from NULL → 라벨만 사라지고 핀·곡선 유지', 'active=false → 노선 자체 소멸', "'99-19'·하나은행 계좌 문자열 0건 grep". 리뷰어 재량을 없앤다.
전원 누락: 목업 3종의 토큰 어휘가 실제로 갈라져 있는데 세 계획 모두 sync 소스를 variant-08 하나로 고정했다. 실측: variant-08 :root 35개 / wizard-b는 variant-08에 없는 12개(--ink --line --muted --muted-2 --shadow --shadow-sm --danger --danger-bg --ok --lav-soft --line-strong --r-sm)를 쓰고 / admin은 없는 18개(--text --text-sub --card --lavender --line-str --r-md --shadow-card --shadow-pop --state-{new,confirm,done,cancel}-{bg,fg} 등)를 쓴다. 따라서 위저드·관리자 이식 시점에 정의되지 않은 커스텀 프로퍼티를 참조하거나(무효값 → 폴백/투명) 구현자가 값을 창작한다(창작 금지 규약 위반). 토큰 어휘 통합·네임스페이스 결정은 컴포넌트 태스크 안이 아니라 UIUX 세션과의 별도 계약 태스크여야 하는데 어느 계획에도 없다. / fail-closed 방어와 프리뷰 E2E 게이트가 서로를 막는데 아무도 해소하지 않았다. Vercel 프리뷰 빌드는 NODE_ENV=production이라 '개발 환경 한정 우회'가 죽고, Upstash·Turnstile이 사장님 명의 등록 대기이면 접수 경로가 전부 막혀 '프리뷰에서 실접수 1건'이라는 게이트(delivery P1·risk P3·legal B)가 영원히 닫히지 않는다. Turnstile 공식 테스트 키(항상 통과)와 개발자 소유 Upstash 인스턴스를 명시적 태스크·env로 두는 계획이 하나도 없다. / 동의의 법적 근거(PIPA §15①1호 동의 vs 4호 계약의 체결·이행)를 어느 계획도 결정하지 않은 채 세 계획 모두 consent_*/privacy_consent_at 타임스탬프 컬럼을 만든다. 근거가 계약 이행이면 기록해야 할 것은 '고지 시각·고지 버전'이고 필수 체크박스는 만들면 안 되며, 근거가 동의면 체크박스와 거부권 안내가 필수다. 이 분기 하나가 마이그레이션·zod 계약·고지 UI를 동시에 결정하는데 세 계획 모두 스키마를 먼저 굳히고 판단을 뒤로 미룬다 — 컬럼은 한 번 채워지면 소급 정정이 불가능하다. / 사장님·컨트롤러가 실행해야 하는 항목(도메인 갱신 D-29, 계정 명의·결제수단 등록, 발신번호 서류, 지급방법·시기 회신)을 어느 계획도 개발 태스크의 차단 조건으로 만들지 않았다. 특히 '목업 동결'은 세 계획 모두가 픽셀 이식 착수 신호로 삼는데(delivery P4-1, risk P6, legal B1~B4) 누가·언제·어떤 형식(커밋 해시? 선언 문서?)으로 선언하는지 정의가 없다. 정의 없는 신호에 계획 후반 전체가 매달려 있다. / Next 런타임 고유 층에 대한 회귀 테스트가 세 계획 모두 0이다 — after()의 실행 보장, revalidateTag 전파, 서버액션 폼 직렬화, 미들웨어+세션 쿠키 갱신, 로케일 프리픽스와 admin 게이트의 상호작용. 이 층은 전부 1회성 browse 실측에 맡겨져 있는데 실측은 회귀 방지력이 없다(한 번 통과 후 다음 커밋에서 깨져도 CI가 모른다). Playwright 등 브라우저 E2E를 CI에 넣는 태스크가 어디에도 없다. / §13.2의 '서울→전주'가 세 계획 모두에서 누락됐다. 신규 도시 목록을 셋 다 동일하게 10개(통영·포항·여수·해남·세종·속초·강릉·태백·홍천·원주)로 잡았는데 전주는 여기에 없고, 결국 기존 시도 코드 JB(전라북도)로 흡수되어 사장님이 말한 '전주'가 화면에 '전라북도'로 뜬다. 도시 단위로 전환하는 마이그레이션의 목적 자체를 16개 중 1개에서 배반한다. / '무엇이 오픈을 막는가'와 '무엇이 개발 완료인가'를 분리해 서명하는 절차가 legal E6에만 한 줄 있고 나머지 둘에는 없다. 세 계획 다 런치 게이트 항목의 절반이 사장님 행동에 의존하는데, 개발자가 통제 가능한 항목만으로 정의된 '개발 준비 완료' 상태를 별도로 선언하지 않으면 마지막 단계가 무기한 미완으로 남고 진척 보고가 불가능해진다.

## 심사 gap
- delivery: 61점
  강점: 세 계획 중 유일하게 [locale] 라우팅 골격을 P0에 넣어, 법정 3라우트·공개 페이지가 나중에 URL을 바꾸지 않는다. risk·legal은 app/privacy를 먼저 만들고 i18n을 뒤에 붙여 전 라우트 이설이 예약돼 있다.; '토큰(sync-tokens) · 카피(messages) · 데이터(DB)' 3접점만으로 목업을 추종하는 계약이 가장 구체적이고, tokens.test.ts가 드리프트를 CI 실패로 만든다.; lib/fallback-routes.ts — DB 장애 시 코드 소유 정적 카탈로그(가격 전부 null)로 폴백해 홈이 빈 지도가 되지 않게 한다. 폴백에 가격을 넣지 않는다는 제약까지 테스트로 못박았다.; P3-2의 '모든 admin 서버액션이 requireAdmin()을 호출하는지' 소스 파싱 테스트, P1-3의 '개발용 우회 플래그가 NODE_ENV=production에서 무시되는지' 테스트 — 규칙이 아니라 기계로 강제한 두 지점.; 각 태스크의 테스트 항목이 가장 구체적이고 실행 가능하다(라벨 숨김을 DB 행 수정으로 실측, 좌표를 선형변환식과 0.1px 이내 대조 등).; P1-6에서 접수 경로를 위저드 하나로 통일 — 동의 UI가 두 벌이 되는 사고를 구조로 차단.
  치명결함: **전제가 사실과 다르다.** P0-1은 Supabase 프로젝트를 새로 프로비저닝(ap-northeast-2)하라고 하고 P0-4는 '아직 프로덕션 DB가 없으므로 지금이 마지막 무통증 시점'이라고 적었다. 실제로는 supabase/.temp/project-ref=expexkhcuogkavpacrem 로 원격 프로젝트가 링크돼 있고, 레저에 '사용자 SQL Editor로 0001 적용, showcase_routes 5행, migration repair로 이력 동기화 완료'가 기록돼 있다. 리전은 Supabase에서 사후 변경 불가라 '서울로 새로 만든다'는 곧 프로젝트 폐기·키 교체·repair 재수행인데 그 절차가 계획에 없다.; **admin이 계속 service role을 쓴다.** RLS가 최종 방어선이 되지 못하고, 유일한 방어가 자기진단조차 우회 가능하다고 인정한 소스 파싱 테스트다. 사장님이 약한 비밀번호를 감수했고(§13.5) MFA 태스크도 없다 → 자격증명 1개 = 고객 성명·전화·이메일 전량. 감사 로그도 0개라 사고 후 조사도 불가.; **보유기간 파기 배치가 계획 전체에 없다.** P1-1에서 /privacy에 보유기간을 게시하는데 그것을 이행하는 코드가 어디에도 없다 → 게시 문구가 그 순간부터 거짓 표시이고 PIPA §21 위반이 누적된다. risk(H8-3)·legal(L9)은 둘 다 갖고 있다.; **P3-2가 자기모순이다.** '미들웨어가 /admin을 매처에서 제외한 채로도 세션 쿠키가 갱신되는지 실측'을 테스트로 걸었는데, 매처가 /admin을 제외하면 미들웨어가 그 경로에서 아예 실행되지 않는다. Supabase refresh token 로테이션이 돌지 않아 사장님이 작업 도중 튕기거나 만료 세션을 들고 다닌다. 통과할 수 없는 게이트를 통과할 것으로 적어놨다.; **통지 유실 설계가 얕다.** notifications_log에 pending 선기록이 없어 after() 안에서 함수가 죽으면 흔적이 0이다. attempts·last_error 컬럼도, 실패 목록 화면도, 재발송 액션도 없다(P3-3의 '재발송 버튼'이 유일한 언급인데 실패 건 조회 경로가 없다). 즉 '사장님이 접수를 놓치지 않는다'는 P2 목표를 P2가 달성하지 못한다.; P0가 16개 노선 가격 라벨을 프리뷰에 먼저 띄우는데, 그 가격이 verbatim 고지('45인승 당일왕복 기준')와 맞는지는 미확인이고 계획도 '틀리면 전부 숨긴다'고만 적었다. 가장 불확실한 것을 가장 먼저 배포한다 — 인천공항→서울 40만원은 성격상 편도 송영이라 어긋날 확률이 낮지 않다.; '99-19'는 grep으로 막았지만 '면허 보유'(P1 결함 16)에 대한 저장소 전역 grep이 없다. 실증 불가 수치 grep은 P4-1의 개별 테스트로만 존재해 게이트가 아니다.
- risk: 69점
  강점: **게이트를 코드보다 먼저 심는다**는 원칙과 '조건부 강제'(대상 파일이 없으면 exit 0, 생기는 순간부터 실패) 설계 — 나중에 게이트를 심을 때 '이미 위반된 코드를 통과시키는' 사고를 구조적으로 없앤다. 세 계획 중 유일.; admin을 service role에서 떼어내 is_admin() RLS로 옮기고(A1-1), admin_audit를 insert-only로 먼저 만들고(A1-4), 목록은 마스킹·상세 열람은 감사 기록(G7-1)까지 간다. 개인정보 폭발반경을 실제로 줄이는 유일한 계획.; 통지 아웃박스(D4-1)가 제대로 됐다: status pending/sent/failed, attempts, last_error, unique(reservation_id,event,channel) where status='sent' 부분 인덱스, 그리고 D4-4의 실패 목록 + 재발송 + force 구분 + audit. '기록 후 발송'이라는 순서가 명시적.; R0-4 server-only 경계 스캐너(‘use client’ 파일의 server.ts import 0건, NEXT_PUBLIC_ 시크릿 0건)와 C3-1 fail-closed(env 부재=거부) — 조용히 무력화되는 두 사고를 테스트로 못박았다.; C3-2: public_code를 비순차로 만들고 KST 날짜 경계를 TZ=UTC/TZ=Asia/Seoul 양쪽에서 실증. 접수번호 열거로 타인 예약을 조회하는 경로를 설계 단계에서 막은 유일한 계획.; H8-1의 statusCode:301 명시(permanent:true와 병용 금지)와 has:[{type:'query'}] 분기, fallback 규칙 순서까지 테스트 선작성으로 규정.; H8-3 파기 배치에서 확정건(전자상거래법 보존기간)과 미확정 접수건의 보유기간을 분리 계산하고 드라이런을 둔 것.
  치명결함: **라우팅 형태를 끝까지 정하지 않는다.** B2-3에서 app/privacy/page.tsx를 만들고, F6-2에서 next-intl을 배선하고, F6-4에서 app/(site)/page.tsx를 도입한다. 즉 P2에서 만든 법정 라우트는 P6에서 로케일 세그먼트/라우트 그룹으로 이설되거나 EN에서 접근 불가로 남는다. '오픈 블로커를 먼저 끝낸다'는 전략의 산출물이 나중에 손을 타야 한다 — 순서 원칙의 자기부정.; **P1 전체가 검증되지 않은 전제 하나에 걸려 있다.** 'admin을 RLS만으로 굴릴 수 있다'는 가정인데 자기진단조차 '집계·크로스테이블에서 결국 service role로 되돌아가는 경우가 흔하다'고 인정한다. 되돌아가면 A1-1~A1-4의 명분이 통째로 사라지는데 폴백 시나리오도, 선행 스파이크도 없다.; **방문자가 볼 수 있는 화면이 P6에서 처음 나온다.** P0~P5 다섯 단계가 전부 비가시 작업이다. 도메인 만료 D-29, 사장님이 진행을 눈으로 확인할 방법 0, 그리고 자기진단이 인정하듯 F6-4(홈 6개 섹션)는 서브에이전트 1회로 끝나지 않을 크기다. 일정 리스크가 뒤에 한 덩어리로 몰려 있다.; E5-1이 '아직 데이터가 없으니 싸다'에 가깝게 서술돼 있으나 0001은 원격 적용 완료(showcase_routes 5행)이고 이 저장소는 migration repair를 이미 한 번 겪었다. 컬럼 교체형 전환을 태스크 1개에 몰아넣었다.; R0-3의 실증 불가 수치 검출이 check-temp-values.sh(항상 exit 0)에 붙고 STRICT=1일 때만 1을 반환한다. CI 어느 잡도 STRICT=1을 켜지 않으므로 기본 상태에서는 차단력이 0이다 — F6-4 게이트에서만 사람이 기억해야 한다.; B2-3의 법정 표시 검사 목록에 만 14세 미만 안내와 소비자 불만·분쟁처리 안내(P1 결함 18)가 빠져 있다. 게이트 문장에 [TEMP]로 스쳐 지나갈 뿐 화면 요건으로 확정되지 않았다. 타사 정보(전세버스하나/645501/1566-3027) grep도 어느 태스크에도 없다.; D4는 재시도 큐를 명시적으로 범위 밖에 두고 '사장님이 admin을 매일 연다'는 미검증 가정에 의존한다. 초기에는 발신번호 미승인으로 확실히 실패한다고 스스로 적어놓고, 실패를 알리는 경로는 admin 화면뿐이다.
- legal: 64점
  강점: **lib/legal/disclosures.ts가 함수를 하나도 export 하지 않는지 테스트로 강제**(Object.values(mod).every(v=>typeof v!=='function')) — 전자상거래법 §13②3호 이행과 CLAUDE.md의 가격 계산 코드 금지를 양립시키는 경계를 코드로 그은 것. 세 계획을 통틀어 가장 좋은 단일 아이디어.; 법정 고지 내용의 정확도가 가장 높다: 위탁을 국내(Solapi)/국외로 분리, 국외이전 5항목 + '이전의 근거' 호수 병기, 만 14세 미만, 불만·분쟁처리, 정책 버전 문자열까지 L5 테스트 항목에 들어 있다.; 형식적 필수동의 체크박스를 만들지 않되 4대 고지는 반드시 노출한다는 L6 설계 — 결정 메모의 권고를 정확히 읽었고, '동의 UI 생략'과 '고지 생략'을 혼동하지 않는다.; lib/codes.ts의 CONTACT_METHODS가 위저드 칩의 유일한 진실이라는 계약(B4·C3) — 목업에 이메일 칩이 남아 있어도 코드 상수가 화면을 결정한다. 목업 추종을 데이터로 환원한 좋은 경계.; L9 파기 배치를 dry-run 기본값으로 두고 오픈 직전에 활성화 — 복구 불가한 오삭제를 기본 안전 상태로 막았다.; 픽셀 이식 브리프에 목업 커밋 해시를 고정하고 이후 해시 구간 diff만 재적용한다는 병행 전략 — UIUX 세션 변경을 흡수하는 가장 구체적인 방법.
  치명결함: **35태스크 6페이즈는 D-29에 대해 명백히 과대하고, 계획 스스로 '중간 산출물은 배포 가능하지만 유용하지 않다'고 인정한다.** Phase L을 다 끝내도 홈·위저드가 없어 오픈이 불가하다. 즉 가장 비싼 순서로 가장 늦게 가치를 낸다.; **L4가 app/layout.tsx에 푸터를 심는데 B1이 나중에 app/(site)/layout.tsx를 도입하고 B6에서 i18n을 배선한다.** 법정 3라우트도 app/privacy로 로케일 밖에 만든다. 결과: 푸터·법정 페이지가 셸 확정 전에 만들어져 재작업이 예약돼 있고(자기진단도 인정), EN 방문자에게 법정 문서·동의 고지가 한국어로만 남는다. §13.3이 외국인 문의를 실재로 확인한 상태라 이건 카피 문제가 아니라 동의 유효성 문제다.; **D2가 '읽기는 SSR 세션, 쓰기만 검증 후 service role'로 설계돼 있다.** service role은 RLS를 우회하므로 D2가 만드는 쓰기 정책은 장식이 되고, 테스트('관리자만 write 허용')는 통과하지만 실제 실행 경로는 그 정책을 타지 않는다. 게다가 감사 로그 태스크가 하나도 없다 — 누가 언제 고객 전화번호를 열었는지 사후에 만들 수 없다.; **발송 실패 복구 경로가 없다.** C1에 중복 가드는 있으나 attempts·last_error·pending 선기록이 없고, admin에 실패 목록도 재발송 액션도 없다(D3은 상태 전이만). 계획 자신이 '발신번호 서류 미수령 → 스텁 모드'라고 적었으므로 초기에는 전 건 실패가 확정인데, 그것을 사람이 발견할 화면이 없다.; **L7이 과대 태스크이면서 fail-closed를 명시하지 않았다.** actions/reservations.ts + lib/rate-limit.ts + lib/turnstile.ts + lib/public-code.ts를 한 태스크에 묶고, Upstash env 부재 시 거부(fail-closed)라는 요건이 테스트 목록에 없다 → 프로덕션에서 RL이 조용히 무력화되는 기본 실패 모드가 열려 있다. public_code의 비순차성 요건도 없어 열거 조회에 취약하다.; L2가 check-temp-values.sh에 실증 수치 패턴을 추가하지만 그 스크립트는 항상 exit 0이고 STRICT 도입도 없다 → 실증 불가 수치는 CI에서 절대 실패하지 않는다. legal-gate 잡은 문자열 '존재'만 본다.; D6이 sharp류 이미지 처리 의존성을 전제하는데 package.json에 없고 Vercel 함수 제약도 미검증(자기진단 인정). §13.9가 heic를 허용 형식으로 못박아 둔 상태라 이 태스크는 착수 시점에 재계획이 확정적이다.
이식할 아이디어: risk A1-4 + G7-1: admin_audit(actor·action·target·at) insert-only 테이블을 첫 admin 조작 이전에 만들고, 상세 화면에서 고객 연락처 원문을 '열람'한 사실까지 기록. 목록은 마스킹 기본. 사고 후에 만들 수 없는 유일한 자산이다. / risk D4-1/D4-4 아웃박스 전체: pending 선기록 → 발송 → 상태 전이, attempts·last_error·next_attempt_at, unique(reservation_id,event,channel) where status='sent' 부분 인덱스, admin 실패 목록 + 재발송(sent는 force 필요) + 재발송도 audit. delivery·legal의 통지 설계를 이것으로 교체해야 한다. / legal lib/legal/disclosures.ts 원장 모듈 + '함수 export 0개' 테스트. §13②3호 이행을 정적 상수로만 구현한다는 경계를 기계로 못박는 방식이며, check:pricing과 반대 방향에서 잠근다. / legal lib/codes.ts CONTACT_METHODS 계약: 위저드 칩·드롭다운의 유일한 진실은 목업이 아니라 코드 상수. 이메일 견적 옵션 제거가 '목업과 다른 화면'이 아니라 '계약대로의 화면'이 된다. / risk R0-4 server-only 경계 스캐너: 'use client' 파일의 lib/supabase/server.ts import 0건, SUPABASE_SERVICE_ROLE_KEY가 server.ts 밖에 0건, NEXT_PUBLIC_ 접두 시크릿 0건. 실패 픽스처로 테스트가 실제 red가 되는지 확인하는 절차까지 포함. / risk C3-1 fail-closed + delivery P1-3의 보완: Upstash/Turnstile env 부재 시 통과가 아니라 거부, 개발 우회 플래그는 NODE_ENV=production에서 무시되는지를 테스트로 못박기, Turnstile 응답의 hostname/action 클레임 검증과 토큰 재사용 거부. / risk C3-2: public_code 비순차 생성 + KST 날짜 경계를 TZ=UTC와 TZ=Asia/Seoul 양쪽에서 실증 + DB unique 이중화. / delivery P0-6: [locale] 라우팅 골격을 첫 화면 코드와 함께 세우고 EN 메시지는 비워 ko로 폴백. 법정 3라우트·푸터가 나중에 이설되지 않게 하는 유일한 방법이며, risk·legal은 반드시 이걸 앞으로 당겨야 한다. / delivery P0-5 lib/fallback-routes.ts: DB 장애 시 코드 소유 정적 카탈로그(가격 전부 null)로 폴백, 폴백 정합성(코드가 전부 카탈로그에 존재)까지 테스트. / delivery P3-2: actions/admin*.ts의 export된 'use server' 함수를 파싱해 requireAdmin() 호출 누락을 검출하는 정적 검사(단, 진짜 방어는 risk의 RLS이므로 둘을 함께 쓸 것). / risk의 '조건부 강제' 게이트 설계: 대상 파일이 없으면 exit 0 + 경고, 생기는 순간부터 실패, LAUNCH_GATE=1이면 항목 부재 자체로 실패. 게이트를 코드보다 먼저 심을 수 있게 하는 유일한 형태. / delivery P0-2 / risk F6-1: 목업 :root에서 styles/tokens.css를 재생성하고 커밋본과 다르면 CI 실패(드리프트 감지) + verbatim 2문구·회사정보 8필드를 목업에서 추출해 messages/ko.json과 대조하는 copy-parity 테스트. / legal L9 + risk H8-3: 파기 배치를 dry-run 기본값으로 만들고, 확정건(전자상거래법 보존기간)과 미확정 접수건의 보유기간을 분리 계산, 삭제 건수를 audit에 기록. / risk H8-1 / legal E1: 301 매핑 테스트 선작성, statusCode:301 명시(permanent:true 병용 금지), has:[{type:'query',key:'bo_table'}] 분기, fallback 규칙이 앞선 규칙을 가리지 않는 순서 검증. / legal L8 / risk D4-2: 문자 템플릿에 광고 유발어('할인·이벤트·특가·프로모션') grep 0건을 CI로 강제 — 한 줄만 섞여도 정보성이 광고성으로 전환된다. / delivery P1-6: 접수 경로를 위저드 하나로 통일(홈 위젯은 프리필만). 동의 UI·검증이 두 벌이 되어 하나가 빠지는 사고를 구조로 제거. / legal의 목업 커밋 해시 고정 브리프 + risk의 '픽셀 이식은 목업 동결 후' 시점 규칙을 함께 적용.
전원 누락: **에러 화면 자체가 없다.** app/error.tsx·global-error.tsx·not-found.tsx·loading.tsx가 세 계획 어디에도 없다. App Router는 이게 없으면 서버 컴포넌트 예외 시 프로덕션에서 digest만 찍힌 기본 오류 화면을 띄운다 — 방문자에게는 사이트가 죽은 것으로 보이고 사장님에게는 전화만 온다. 404 페이지도 없는데 P5/P8/E1의 리다이렉트 계획은 404 도달을 전제한다. / **관측이 0이다.** 에러 리포팅(Sentry 또는 최소한 Vercel Log Drain), 요청 상관 ID, 404 로그, /api/health, 업타임 모니터가 전부 없다. delivery P5-1은 '오픈 후 404 로그로 리다이렉트를 보강한다'고 적었는데 그 로그를 만드는 태스크가 없고, risk·legal도 실패를 사람이 admin을 열어야만 아는 구조다. / **발송 실패를 알리는 2차 채널이 없다.** 사장님 알림 문자 자체가 실패하는 것이 가장 흔한 실패인데(C1 미개통·발신번호 미승인이 확정 상태), 그 실패를 알리는 수단이 다시 그 문자뿐이다. 메일 폴백·일일 다이제스트·admin 미확인 배지 중 무엇도 없다. 더해 **Solapi는 선불 잔액제인데 잔액 소진 감시가 세 계획 모두 0**이다 — 잔액 0이 되는 순간 전 건이 조용히 실패한다. / **아웃바운드 SMS 유량 상한(서킷 브레이커)이 없다.** IP를 로테이션하는 봇이 접수를 뿌리면 건당 실비(LMS 45원)와 사장님 폰 폭탄이 그대로 발생한다. IP별 RL만으로는 못 막는다. 시간당/일당 전역 발송 상한 + 초과 시 접수는 받되 발송만 큐잉하는 설계가 필요하다. / **rate limit 키의 IP 추출 방식이 어느 계획에도 규정돼 있지 않다.** Vercel에서 x-forwarded-for를 순진하게 파싱하면 클라이언트가 헤더를 위조해 키 공간을 무한히 만들 수 있어 RL이 사실상 무력화된다. 신뢰 가능한 프록시 헤더를 명시하고 위조 XFF로 우회되지 않는지 테스트해야 한다. / **createReservation에 멱등성이 없다.** 더블 클릭·네트워크 재시도·뒤로가기 재제출이 곧 2행 + 2건 문자다. 클라이언트 발급 idempotency key + unique 제약이 세 계획 모두 없다(중복 가드는 reservation_id+event 단위라 서로 다른 두 행은 못 막는다). / **Supabase 프로젝트 리전을 확인하는 태스크가 없다.** 이미 원격 프로젝트가 링크돼 있고(project-ref: expexkhcuogkavpacrem, 0001 적용·repair 완료) 리전은 사후 변경이 불가하다. 국외이전 고지 범위와 지연 시간이 여기서 갈리는데, delivery는 확인 없이 신규 프로비저닝을 지시하고 risk·legal은 아예 언급이 없다. '지금 확인하고 필요하면 지금 재생성'이 마지막 저비용 시점이다. / **새 시스템의 백업·복구가 없다.** 구 그누보드 백업만 있고, Supabase의 일일 백업/PITR 플랜 확인, 복구 리허설, Storage(갤러리 원본) 백업, 사장님용 예약 CSV 내보내기가 전부 없다. 사장님이 폰에서 원본을 지우고 업로드하면 Storage가 유일본이 된다. / **계정 소유권·결제수단·요금제 등급이 태스크가 아니다.** Vercel Hobby는 상업적 사용 불가이고 cron이 1일 1회로 제한되며, Supabase 일일 백업은 Pro가 필요하다(§13.9가 이미 Pro $25를 전제). legal L9·risk H8-3의 cron과 §13.9의 백업 전제가 플랜 등급에 걸려 있는데 검증 태스크가 없다. 소유권 이전은 delivery만 런북으로 언급. / **프리뷰 배포에 noindex가 없다.** 세 계획 모두 프리뷰 URL을 사장님에게 보내고 실접수까지 받는데, Vercel 프리뷰는 x-robots-tag를 세우지 않으면 색인 대상이다. 오픈 전에 중복 사이트가 색인되면 본 도메인의 색인 이관이 꼬인다. / **rel=canonical이 셋 다 0건이다.** hreflang만 있고 canonical이 없는데, apex/www·localePrefix 'as-needed'로 인한 /와 /ko·trailing slash·리다이렉트에 따라붙는 원 쿼리스트링이 전부 중복 URL을 만든다. apex와 www 중 무엇이 정본인지도 결정돼 있지 않다. / **/bbs/:path* → 홈 일괄 fallback은 대량 소프트404 패턴이다.** 세 계획 모두 미매핑 URL을 홈으로 보내는데, 이는 2013년부터 쌓인 색인 자산을 지키는 게 아니라 오히려 잃는 방식이다. 매핑 실패는 404 + 안내가 정석이고, 어느 계획도 이 트레이드오프를 다루지 않는다. 더해 네이버 서치어드바이저·구글 서치콘솔 사이트 등록/sitemap 제출/이관 후 색인 모니터링 태스크가 없다(메모 5절의 미확인 항목이 계획으로 승계되지 않았다). / **접근성이 Lighthouse 점수 한 줄로 대체됐다.** 6단계 위저드의 키보드 전용 조작, 단계 전환 시 포커스 이동, 에러 메시지의 aria-live/label 연결, skip-to-content, 모바일 메뉴·팝업의 포커스 트랩(delivery P3-5만 팝업 하나 언급), prefers-reduced-motion, 지도 SVG의 텍스트 대체(스크린리더에는 곡선이 아무 의미가 없다)가 어느 태스크에도 없다. 게다가 POINT #7B7A7A는 4.28:1로 AA 미달이 **이미 확정된 사실**인데(§13.11), 대비비를 CI로 검사하는 장치가 셋 다 없다 — risk는 --gray/--gray-text 분리를 UIUX 세션에 '전달만' 하고 손을 뗀다. / **관리자 계정 보호 수단이 없다.** MFA, 로그인 시도 제한, IP 제한이 셋 다 0건이다. 사장님이 약한 비밀번호를 감수했고(§13.5) 그 계정 하나가 고객 PII 전량에 접근한다. 안전성 확보조치 기준이 요구하는 접속기록 보관(6개월)도 risk의 audit 테이블이 부분적으로 걸칠 뿐 명시된 곳이 없다. / **이미지 파이프라인의 실제 함정을 아무도 안 짚었다.** ① EXIF GPS 제거 — 폰 원본에는 차고·자택 좌표가 박혀 있고 그대로 공개 버킷에 올라간다. ② EXIF 회전 보정 없으면 세로 사진이 눕는다. ③ HEIC 디코드 — §13.9가 허용 형식에 heic를 명시했는데 브라우저 canvas는 HEIC를 못 읽고 Vercel의 기본 sharp 빌드도 지원하지 않는다. 세 계획 모두 '1600/800/400 WebP 3종'만 적고 변환 위치조차 정하지 않았다. 더해 Storage 버킷 정책(비공개 원본/공개 변환본)의 RLS와 업로드 권한 경로가 어느 계획에도 태스크로 없다. / **시간대 경계에 이미 박힌 버그를 셋 다 못 봤다.** 0001의 popups RLS가 `current_date between starts_at and ends_at`인데 Supabase 세션 TZ는 UTC라 팝업이 KST 자정이 아니라 **오전 9시**에 켜지고 꺼진다. notices.published_at default current_date도 같다. delivery P3-5와 legal D5는 '경계일 포함 여부'를 테스트하겠다면서 이 원인을 짚지 못했다. 추가로 Vercel cron은 UTC 기준인데 파기 배치의 KST 실행 시각을 아무도 고정하지 않았고, '오늘 하루 보지 않기'의 자정 기준도 브라우저 로컬이면 해외 접속자(§13.3이 실재로 확인한 외국인 문의)에게 어긋난다. / **개인정보 사고 대응 절차가 없다.** 유출 인지 후 72시간 내 통지·신고 런북, 키 유출 시 로테이션 절차(service role·Solapi·Upstash 키를 어디서 어떤 순서로 갈아끼우는지)가 세 계획 모두 없다. risk가 '키 폭발반경'을 전략의 축으로 삼으면서도 정작 유출 후 절차는 없다. / **회귀력 있는 E2E가 없다.** 모든 UI 게이트가 1회성 browse 실측인데, 실측은 다음 커밋에서 깨져도 CI가 모른다(delivery만 자기진단으로 인정하고 대책은 없다). 접수 1건이 도는 최소 Playwright 스위트조차 어느 계획에도 태스크로 없다. / **방문자 분석 도구를 넣을지 여부가 결정되지 않았다.** 사장님은 반드시 '몇 명 들어왔나'를 묻는다. 붙이는 순간 처리방침에 자동수집 항목·쿠키 고지가 필요한데, 세 계획의 /privacy 항목 목록에 쿠키·자동수집이 없다. Cloudflare Turnstile이 심는 쿠키/식별자도 마찬가지로 미고지다. / **사장님 운영 매뉴얼이 산출물에 없다.** '사장님이 스스로 운영한다'가 P3/P7/D의 목표인데, 노선 추가·가격을 비운다는 것의 의미(라벨 숨김 ≠ 비활성)·문자 재발송·사진 업로드·팝업 기간 설정을 설명하는 문서가 어느 계획에도 없다. 동석 E2E 1회로 대체하고 있다. / **변호사/개인정보 전문가 검토가 태스크가 아니다.** 세 계획 모두 자기진단에서 '필요하다'고 인정만 하고 게이트 항목으로 올리지 않았다. 결정 메모 스스로가 '상담 시 안내 표기의 §13②3호 위반 여부는 심결례 확인 실패, 조문 구조에 근거한 해석'이라고 적어 둔 상태다. 게이트는 문자열 존재만 보므로 '적혀 있지만 불충분한 문구'가 통과한다. / **EN 법정 문서·동의 고지가 사실상 비어 있다.** delivery P4-3만 verbatim의 EN 처리를 미결로 남겼고, risk·legal은 법정 라우트를 로케일 밖에 만들어 EN 방문자에게 한국어 고지만 노출된다. 외국인 접수가 실재한다면(§13.3) 이해할 수 없는 언어의 고지로 받은 동의는 유효성 자체가 흔들린다.